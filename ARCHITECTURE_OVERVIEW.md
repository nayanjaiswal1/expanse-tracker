# 🏗️ System Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           FRONTEND (React + TypeScript)                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                        NAVIGATION LAYER                           │  │
│  │  ┌────────────┐  ┌─────────────────┐  ┌──────────────────────┐  │  │
│  │  │  Sidebar   │  │   Breadcrumbs   │  │   Quick Search       │  │  │
│  │  │  - Dashboard│  │   - Dynamic     │  │   - Cmd+K           │  │  │
│  │  │  - Txns    │  │   - Context-    │  │   - Global search   │  │  │
│  │  │  - Budgets │  │     aware       │  │                      │  │  │
│  │  └────────────┘  └─────────────────┘  └──────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                         MAIN PAGES                                │  │
│  │                                                                    │  │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐  │  │
│  │  │   Dashboard      │  │   Statements     │  │   Budgets     │  │  │
│  │  │  - Charts        │  │  - Card Carousel │  │  - Templates  │  │  │
│  │  │  - Filters       │  │  - Upload UI     │  │  - AI Gen     │  │  │
│  │  │  - Summaries     │  │  - Comparison    │  │  - Tracking   │  │  │
│  │  └──────────────────┘  └──────────────────┘  └───────────────┘  │  │
│  │                                                                    │  │
│  │  ┌──────────────────────────────────────────────────────────────┐ │  │
│  │  │            TRANSACTIONS PAGE (MAIN FEATURE)                   │ │  │
│  │  │  ┌────────────────────┐  ┌──────────────────────────────────┐│ │  │
│  │  │  │  Transaction List  │  │  Chat Interface (NEW)            ││ │  │
│  │  │  │  - Filter panel    │  │  ┌────────────────────────────┐ ││ │  │
│  │  │  │  - Sort options    │  │  │  Message List              │ ││ │  │
│  │  │  │  - Bulk actions    │  │  │  - User messages          │ ││ │  │
│  │  │  │                    │  │  │  - System responses       │ ││ │  │
│  │  │  │  ┌──────────────┐ │  │  │  - Transaction cards      │ ││ │  │
│  │  │  │  │ Inline       │ │  │  └────────────────────────────┘ ││ │  │
│  │  │  │  │ Assistant    │ │  │  ┌────────────────────────────┐ ││ │  │
│  │  │  │  │ - Suggestions│ │  │  │  Chat Input               │ ││ │  │
│  │  │  │  │ - Context    │ │  │  │  - @ mention autocomplete │ ││ │  │
│  │  │  │  └──────────────┘ │  │  │  - File drop zone         │ ││ │  │
│  │  │  └────────────────────┘  │  │  - AI/Normal mode toggle  │ ││ │  │
│  │  │                          │  └────────────────────────────┘ ││ │  │
│  │  └──────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      STATE MANAGEMENT                             │  │
│  │  ┌──────────┐  ┌───────────┐  ┌────────────┐  ┌──────────────┐  │  │
│  │  │React Query│ │  Contexts  │  │ Local State│  │  URL State   │  │  │
│  │  │- Server   │ │  - Auth    │  │  - Forms   │  │  - Filters   │  │  │
│  │  │  data     │ │  - Theme   │  │  - UI      │  │  - Selected  │  │  │
│  │  │- Cache    │ │  - Chat    │  │            │  │    items     │  │  │
│  │  └──────────┘  └───────────┘  └────────────┘  └──────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ REST API (JSON)
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         BACKEND (Django + DRF)                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                          API LAYER                                │  │
│  │  ┌────────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │  │
│  │  │ Transaction│  │ Statement│  │   Chat   │  │    Budget     │  │  │
│  │  │  ViewSets  │  │ ViewSets │  │ ViewSets │  │   ViewSets    │  │  │
│  │  │            │  │          │  │          │  │               │  │  │
│  │  │ - CRUD     │  │ - Upload │  │ - Parse  │  │ - Templates   │  │  │
│  │  │ - Filter   │  │ - Compare│  │ - Mention│  │ - AI Generate │  │  │
│  │  │ - Classify │  │ - Merge  │  │ - Save   │  │               │  │  │
│  │  └────────────┘  └──────────┘  └──────────┘  └───────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                       BUSINESS LOGIC                              │  │
│  │  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────┐  │  │
│  │  │  Serializers   │  │   Permissions    │  │   Validators    │  │  │
│  │  │  - Validation  │  │  - Row-level     │  │  - Custom rules │  │  │
│  │  │  - Transform   │  │  - User-based    │  │  - Business     │  │  │
│  │  └────────────────┘  └──────────────────┘  └─────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      SERVICE LAYER                                │  │
│  │  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────┐  │  │
│  │  │  AI Services   │  │  File Services   │  │  Integration    │  │  │
│  │  │  - OpenAI      │  │  - PDF parser    │  │  Services       │  │  │
│  │  │  - Claude      │  │  - OCR           │  │  - Gmail        │  │  │
│  │  │  - Gemini      │  │  - Encryption    │  │  - Splitwise    │  │  │
│  │  │  - Fallback    │  │  - Dedup detect  │  │  - Telegram     │  │  │
│  │  └────────────────┘  └──────────────────┘  └─────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                     DATA LAYER (Models)                           │  │
│  │                                                                    │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐  │  │
│  │  │  finance_v2  │  │   finance    │  │   users                │  │  │
│  │  │              │  │              │  │                        │  │  │
│  │  │ - Transaction│  │ - Category   │  │ - User                 │  │  │
│  │  │ - Group      │  │ - Budget     │  │ - UserProfile          │  │  │
│  │  │ - Split      │  │ - Goal       │  │ - AISettings           │  │  │
│  │  │ - Upload     │  │ - Tag        │  │ - UserPreferences      │  │  │
│  │  │              │  │              │  │                        │  │  │
│  │  │ NEW:         │  │ NEW:         │  │                        │  │  │
│  │  │ - ChatMsg    │  │ - Budget     │  │                        │  │  │
│  │  │ - StmtPass   │  │   Template   │  │                        │  │  │
│  │  │ - StmtLog    │  │              │  │                        │  │  │
│  │  │ - Mention    │  │              │  │                        │  │  │
│  │  │ - StmtMerge  │  │              │  │                        │  │  │
│  │  └──────────────┘  └──────────────┘  └────────────────────────┘  │  │
│  │                                                                    │  │
│  │  ┌──────────────┐  ┌──────────────┐                              │  │
│  │  │  services    │  │  reference   │                              │  │
│  │  │              │  │              │                              │  │
│  │  │ - Gmail      │  │ - Currency   │                              │  │
│  │  │ - Splitwise  │  │ - Country    │                              │  │
│  │  │ - Patterns   │  │ - Timezone   │                              │  │
│  │  └──────────────┘  └──────────────┘                              │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    BACKGROUND TASKS (Celery)                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌────────────────────┐  ┌────────────────────┐  ┌──────────────────┐  │
│  │  Chat Processing   │  │ Statement Tasks    │  │  Budget Tasks    │  │
│  │                    │  │                    │  │                  │  │
│  │ - Parse message    │  │ - Try passwords    │  │ - Generate from  │  │
│  │ - Extract entities │  │ - Detect dupes     │  │   template       │  │
│  │ - Create txn       │  │ - Merge files      │  │ - AI suggestion  │  │
│  │ - Process files    │  │ - Generate compare │  │                  │  │
│  └────────────────────┘  └────────────────────┘  └──────────────────┘  │
│                                                                           │
│  ┌────────────────────┐  ┌────────────────────┐  ┌──────────────────┐  │
│  │  Email Sync        │  │ AI Processing      │  │  Notifications   │  │
│  │                    │  │                    │  │                  │  │
│  │ - Gmail fetch      │  │ - Batch inference  │  │ - Email alerts   │  │
│  │ - Parse emails     │  │ - Retry failed     │  │ - Telegram msgs  │  │
│  │ - Extract txns     │  │ - Cost tracking    │  │                  │  │
│  └────────────────────┘  └────────────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       DATA STORAGE & CACHING                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────────┐    │
│  │   PostgreSQL     │  │      Redis       │  │   S3 / FileSystem  │    │
│  │                  │  │                  │  │                    │    │
│  │ - User data      │  │ - Session cache  │  │ - Uploaded files   │    │
│  │ - Transactions   │  │ - API responses  │  │ - Statements       │    │
│  │ - Accounts       │  │ - Celery queue   │  │ - Receipts         │    │
│  │ - Budgets        │  │ - Rate limiting  │  │ - Documents        │    │
│  │ - Chat history   │  │                  │  │                    │    │
│  └──────────────────┘  └──────────────────┘  └────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       EXTERNAL SERVICES                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │   OpenAI     │  │   Anthropic  │  │    Google    │  │  Splitwise │  │
│  │   GPT-4      │  │   Claude 3.5 │  │    Gemini    │  │    API     │  │
│  │              │  │              │  │              │  │            │  │
│  │ - Parse text │  │ - Parse docs │  │ - Parse text │  │ - Sync     │  │
│  │ - Classify   │  │ - Classify   │  │ - Classify   │  │   groups   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘  │
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐                                     │
│  │    Gmail     │  │   Telegram   │                                     │
│  │     API      │  │     Bot      │                                     │
│  │              │  │              │                                     │
│  │ - Fetch      │  │ - Notify     │                                     │
│  │   emails     │  │ - Commands   │                                     │
│  └──────────────┘  └──────────────┘                                     │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Chat-Based Transaction Entry

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    USER INTERACTION FLOW                                  │
└─────────────────────────────────────────────────────────────────────────┘

1. User Types Message
   │
   ├─→ "@john $50 lunch at pizza place"
   │   - ChatInput component captures input
   │   - @ triggers MentionAutocomplete
   │   - Fuzzy search for "john" in users/groups/categories
   │
   ▼

2. Send to Backend
   │
   ├─→ POST /api/v1/chat/messages/
   │   {
   │     "conversation_id": "main",
   │     "content": "@john $50 lunch at pizza place",
   │     "message_type": "user"
   │   }
   │
   ▼

3. Backend Processing
   │
   ├─→ ChatMessageViewSet.create()
   │   - Save message with status='processing'
   │   - Dispatch Celery task: parse_chat_message_with_ai.delay(message_id)
   │
   ▼

4. Celery Task: Parse Message
   │
   ├─→ parse_chat_message_with_ai(message_id)
   │   - Load user's AI settings
   │   - Call AI provider (OpenAI/Claude/Gemini)
   │   - Prompt: "Extract transaction details from: {@john $50 lunch}"
   │   - AI Response:
   │     {
   │       "amount": 50,
   │       "currency": "USD",
   │       "description": "lunch at pizza place",
   │       "category": "dining",
   │       "mentions": [{"type": "user", "text": "@john", "user_id": 5}],
   │       "transaction_type": "group",
   │       "confidence": 0.95
   │     }
   │   - Update message.metadata with parsed data
   │   - Update message.status = 'completed'
   │
   ▼

5. Return Suggestion to Frontend
   │
   ├─→ WebSocket notification (or polling)
   │   - Frontend receives updated message
   │   - Render TransactionSuggestionCard component
   │   - Show parsed fields with edit options
   │
   ▼

6. User Confirms/Edits
   │
   ├─→ User taps suggestion card
   │   - Modal/inline editor opens
   │   - User can modify amount, category, splits, etc.
   │   - User clicks "Save"
   │
   ▼

7. Save Transaction
   │
   ├─→ POST /api/v1/chat/messages/{id}/save/
   │   - Create Transaction record
   │   - Create TransactionMention records
   │   - If group expense: create TransactionSplit records
   │   - Link transaction to chat message
   │   - Update message.related_transaction
   │
   ▼

8. Update UI
   │
   ├─→ React Query invalidates transaction list
   │   - Transaction appears in main list
   │   - Chat message shows "✓ Saved" badge
   │   - Dashboard updates in real-time
   │
   └─→ DONE
```

---

## Data Flow: Statement Upload & Processing

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    STATEMENT PROCESSING FLOW                              │
└─────────────────────────────────────────────────────────────────────────┘

1. User Selects Account Card
   │
   ├─→ AccountCardCarousel shows all accounts
   │   - User swipes to HDFC Bank account
   │   - Clicks "Upload Statement"
   │
   ▼

2. Upload Modal Opens
   │
   ├─→ StatementUploadModal component
   │   - File drop zone
   │   - Parser selector: [System] [AI - OpenAI] [AI - Claude]
   │   - Password field (optional)
   │   - "Try saved passwords" checkbox
   │
   ▼

3. User Drops PDF File
   │
   ├─→ File: hdfc_statement_jan_2024.pdf (password-protected)
   │   - User selects "AI - Claude"
   │   - Checks "Try saved passwords"
   │   - Clicks "Upload & Parse"
   │
   ▼

4. Frontend Upload
   │
   ├─→ POST /api/v1/statements/upload/
   │   FormData:
   │   - file: <PDF blob>
   │   - account_id: 123
   │   - parser_type: 'ai'
   │   - ai_provider: 'claude'
   │   - try_saved_passwords: true
   │
   ▼

5. Backend: Save File
   │
   ├─→ StatementUploadViewSet.create()
   │   - Save to UploadedFile model
   │   - status = 'processing'
   │   - Dispatch Celery task: process_statement.delay(file_id)
   │   - Return file_id to frontend
   │
   ▼

6. Celery Task: Process Statement
   │
   ├─→ process_statement(file_id)
   │   │
   │   ├─→ STAGE 1: Password Decryption
   │   │   - Log: StatementProcessingLog (stage='password_decrypt')
   │   │   - Try saved passwords sequentially
   │   │   - Password "01011990" works!
   │   │   - Update: used_password_id, is_password_protected=True
   │   │   - Log: status='success', execution_time_ms=150
   │   │
   │   ├─→ STAGE 2: Text Extraction
   │   │   - Log: StatementProcessingLog (stage='text_extraction')
   │   │   - Extract raw text using PyPDF2
   │   │   - Store in uploaded_file.raw_text_extracted
   │   │   - Log: status='success', execution_time_ms=320
   │   │
   │   ├─→ STAGE 3: AI Parsing
   │   │   - Log: StatementProcessingLog (stage='ai_parse')
   │   │   - Call Claude API with raw text
   │   │   - Prompt: "Extract transactions from this bank statement..."
   │   │   - AI returns structured JSON:
   │   │     {
   │   │       "account_number": "****1234",
   │   │       "period": {"start": "2024-01-01", "end": "2024-01-31"},
   │   │       "transactions": [
   │   │         {
   │   │           "date": "2024-01-05",
   │   │           "description": "PIZZA HUT DOWNTOWN",
   │   │           "amount": -45.50,
   │   │           "category": "dining",
   │   │           "confidence": 0.98
   │   │         },
   │   │         ...
   │   │       ]
   │   │     }
   │   │   - Store in uploaded_file.parsed_json
   │   │   - Log: status='success', details=AI response, execution_time_ms=3450
   │   │
   │   ├─→ STAGE 4: Duplicate Detection
   │   │   - Log: StatementProcessingLog (stage='deduplication')
   │   │   - Compare with existing files:
   │   │     - Same account
   │   │     - Overlapping date range
   │   │     - Similar transaction count
   │   │   - Found duplicate: hdfc_statement_jan_2024_old.pdf (95% match)
   │   │   - Create StatementMerge suggestion
   │   │   - Log: status='success', details={'duplicate_count': 1}
   │   │
   │   ├─→ STAGE 5: Transaction Creation
   │   │   - Log: StatementProcessingLog (stage='transaction_creation')
   │   │   - Create Transaction records from parsed_json
   │   │   - Link to uploaded_file
   │   │   - Auto-categorize using AI confidence
   │   │   - Log: status='success', details={'created': 15, 'skipped': 0}
   │   │
   │   └─→ STAGE 6: Completion
   │       - Log: StatementProcessingLog (stage='completion')
   │       - Update uploaded_file.status = 'completed'
   │       - Send notification to user
   │       - Log: status='success'
   │
   ▼

7. Frontend: Show Results
   │
   ├─→ Polling or WebSocket update
   │   - StatementList refreshes
   │   - New file appears with badges:
   │     [✓ Parsed] [🔒 Password Protected] [⚠️ Duplicate Found]
   │   - User clicks file
   │
   ▼

8. Comparison View Opens
   │
   ├─→ GET /api/v1/statements/files/{id}/comparison/
   │   Returns:
   │   {
   │     "raw_text": "HDFC BANK\nStatement Period: 01-Jan-2024...",
   │     "parsed_data": [...],
   │     "confidence_scores": [...],
   │     "differences": [
   │       {
   │         "field": "amount",
   │         "raw": "45.50 CR",
   │         "parsed": -45.50,
   │         "needs_review": false
   │       }
   │     ]
   │   }
   │
   ├─→ StatementComparison component renders:
   │   ┌─────────────────────┬─────────────────────┐
   │   │   Raw Text          │   Parsed Data       │
   │   ├─────────────────────┼─────────────────────┤
   │   │ 05-JAN-2024         │ Date: 2024-01-05    │
   │   │ PIZZA HUT DOWNTOWN  │ Desc: Pizza Hut     │
   │   │ 45.50 CR            │ Amt: -45.50 ✓       │
   │   │                     │ Cat: Dining (98%)   │
   │   └─────────────────────┴─────────────────────┘
   │
   ▼

9. Handle Duplicates
   │
   ├─→ DuplicateStatementAlert shows:
   │   "This statement matches hdfc_statement_jan_2024_old.pdf (95%)"
   │   [Merge] [Keep Both] [Delete Old]
   │
   ├─→ User clicks "Merge"
   │   POST /api/v1/statements/files/{id}/merge/
   │   - Mark old file as duplicate_of=new_file
   │   - Deduplicate transactions
   │   - Update StatementMerge.status = 'completed'
   │
   └─→ DONE
```

---

## Database Relationships Diagram

```
┌──────────────┐         ┌──────────────┐
│     User     │────────<│ ChatMessage  │
└──────────────┘         └──────────────┘
       │                        │
       │                        │ related_transaction
       │                        ▼
       │                 ┌──────────────┐
       │                 │ Transaction  │
       │                 └──────────────┘
       │                        │
       │                        │ mentions
       │                        ▼
       │                 ┌──────────────────┐
       │                 │TransactionMention│
       │                 └──────────────────┘
       │
       ├────────<┌──────────────────┐
       │         │StatementPassword │
       │         └──────────────────┘
       │                 │
       │                 │ used_password
       │                 ▼
       ├────────<┌──────────────┐
       │         │UploadedFile  │───────<┌──────────────────────────┐
       │         └──────────────┘        │StatementProcessingLog    │
       │                 │               └──────────────────────────┘
       │                 │ duplicate_of
       │                 ▼
       │         ┌──────────────┐
       │         │UploadedFile  │
       │         └──────────────┘
       │                 │
       │                 ▼
       │         ┌──────────────┐
       │         │StatementMerge│
       │         └──────────────┘
       │
       ├────────<┌──────────────┐
       │         │   Budget     │
       │         └──────────────┘
       │                 │ template_source
       │                 ▼
       │         ┌──────────────────┐
       │         │ BudgetTemplate   │
       │         └──────────────────┘
       │
       ├────────<┌──────────────┐
       │         │  AISettings  │
       │         └──────────────┘
       │
       └────────<┌──────────────────┐
                 │ UserPreferences  │
                 └──────────────────┘


┌──────────────┐         ┌──────────────┐
│ Transaction  │────────<│ TransactionSplit│
└──────────────┘         └──────────────────┘
       │                        │
       │                        ▼
       │                 ┌──────────────┐
       │                 │    Group     │
       │                 └──────────────┘
       │
       ├────────<┌──────────────┐
       │         │   Category   │
       │         └──────────────┘
       │
       ├────────<┌──────────────┐
       │         │     Tag      │
       │         └──────────────┘
       │
       └────────<┌──────────────┐
                 │   Account    │
                 └──────────────┘
```

---

## Key Component Interactions

### Chat Interface → Transaction Creation

```
ChatInput
   │
   ├─→ User types: "@john $50 lunch"
   │
   ▼
MentionAutocomplete
   │
   ├─→ Searches users: "john" → John Doe (id: 5)
   │
   ▼
ChatInput (with mention)
   │
   ├─→ Sends to backend
   │
   ▼
Backend: ChatMessageViewSet
   │
   ├─→ Saves ChatMessage
   ├─→ Dispatches Celery task
   │
   ▼
Celery: parse_chat_message_with_ai
   │
   ├─→ Calls Claude API
   ├─→ Extracts structured data
   ├─→ Updates message.metadata
   │
   ▼
Frontend: WebSocket update
   │
   ├─→ ChatMessageList re-renders
   │
   ▼
TransactionSuggestionCard
   │
   ├─→ Shows parsed data
   ├─→ User clicks "Save"
   │
   ▼
Backend: ChatMessageViewSet.save()
   │
   ├─→ Creates Transaction
   ├─→ Creates TransactionMention (links @john)
   ├─→ Creates TransactionSplit (if group)
   │
   ▼
React Query invalidation
   │
   ├─→ Transaction list updates
   ├─→ Dashboard updates
   └─→ Chat shows "✓ Saved"
```

---

### Statement Upload → Comparison View

```
AccountCardCarousel
   │
   ├─→ User selects HDFC account
   │
   ▼
StatementUploadModal
   │
   ├─→ User drops PDF file
   ├─→ Selects "AI - Claude"
   ├─→ Checks "Try saved passwords"
   │
   ▼
Backend: StatementUploadViewSet
   │
   ├─→ Saves UploadedFile
   ├─→ Dispatches Celery task
   │
   ▼
Celery: process_statement
   │
   ├─→ Try StatementPassword records
   ├─→ Extract raw text → uploaded_file.raw_text_extracted
   ├─→ Call Claude API → uploaded_file.parsed_json
   ├─→ Detect duplicates → StatementMerge
   ├─→ Create Transaction records
   ├─→ Log every stage → StatementProcessingLog
   │
   ▼
Frontend: StatementList update
   │
   ├─→ Shows new file with badges
   ├─→ User clicks file
   │
   ▼
StatementComparison
   │
   ├─→ Fetches /api/v1/statements/files/{id}/comparison/
   ├─→ Renders side-by-side view:
   │   - Left: raw_text_extracted
   │   - Right: parsed_json
   ├─→ Highlights differences
   │
   ▼
User reviews and confirms
   │
   ├─→ If duplicates found:
   │   DuplicateStatementAlert → Merge action
   │
   └─→ Transactions appear in main list
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     AUTHENTICATION LAYER                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────┐         ┌─────────────────────────────┐    │
│  │  Google OAuth  │────────>│  JWT Token (HttpOnly Cookie)│    │
│  └────────────────┘         └─────────────────────────────┘    │
│                                       │                          │
│                                       ▼                          │
│                             ┌──────────────────┐                │
│                             │ User Session     │                │
│                             │ (Redis cached)   │                │
│                             └──────────────────┘                │
└─────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHORIZATION LAYER                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           DRF Permissions (IsAuthenticated)              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                   │
│                             ▼                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │        Row-Level Security (user filtering)               │  │
│  │  - Transaction.objects.filter(user=request.user)         │  │
│  │  - ChatMessage.objects.filter(user=request.user)         │  │
│  │  - StatementPassword.objects.filter(user=request.user)   │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATA ENCRYPTION                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │         Fernet Symmetric Encryption                    │    │
│  │  - StatementPassword.encrypted_password               │    │
│  │  - AISettings.openai_api_key                          │    │
│  │  - AISettings.claude_api_key                          │    │
│  │  - AISettings.gemini_api_key                          │    │
│  │                                                         │    │
│  │  Key: settings.STATEMENT_PASSWORD_KEY (env var)       │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │         File Encryption (optional, S3 server-side)     │    │
│  │  - Uploaded bank statements                            │    │
│  │  - Receipt images                                      │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      RATE LIMITING                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Django Ratelimit (Redis-backed)                       │    │
│  │  - Burst: 200 requests/minute                          │    │
│  │  - Sustained: 10,000 requests/day                      │    │
│  │  - AI endpoints: 50 requests/hour                      │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Scalability Strategy

### Horizontal Scaling

```
┌──────────────────────────────────────────────────────────┐
│                  LOAD BALANCER (Nginx)                    │
└──────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  Django App  │  │  Django App  │  │  Django App  │
│  Instance 1  │  │  Instance 2  │  │  Instance 3  │
└──────────────┘  └──────────────┘  └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                  │
        ▼                                  ▼
┌──────────────────┐            ┌──────────────────┐
│   PostgreSQL     │            │   Redis Cluster  │
│   (Primary +     │            │   (Session +     │
│    Read Replicas)│            │    Cache)        │
└──────────────────┘            └──────────────────┘
```

### Celery Workers

```
┌──────────────────────────────────────────────────────────┐
│                    Redis (Broker)                         │
└──────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┬──────────────┐
        │                │                │              │
        ▼                ▼                ▼              ▼
┌──────────────┐  ┌──────────────┐  ┌─────────┐  ┌─────────┐
│ AI Workers   │  │ File Workers │  │  Email  │  │  Misc   │
│ (GPU-enabled)│  │ (CPU-heavy)  │  │ Workers │  │ Workers │
│              │  │              │  │         │  │         │
│ - Parse chat │  │ - OCR        │  │ - Gmail │  │ - Notify│
│ - Classify   │  │ - PDF parse  │  │  sync   │  │ - Export│
│ - Suggest    │  │ - Dedup      │  │         │  │         │
└──────────────┘  └──────────────┘  └─────────┘  └─────────┘
```

### Caching Strategy

```
┌─────────────────────────────────────────────────────────┐
│                    CACHE LAYERS                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Browser Cache (React Query)                         │
│     - Transaction list: 5 min stale time                │
│     - Account data: 10 min stale time                   │
│     - Reference data: 1 hour stale time                 │
│                                                          │
│  2. CDN Cache (Static Assets)                           │
│     - JS bundles: 1 year                                │
│     - Images: 1 month                                   │
│     - Fonts: 1 year                                     │
│                                                          │
│  3. Redis Cache (API Responses)                         │
│     - Dashboard summaries: 5 min                        │
│     - Budget allocations: 10 min                        │
│     - AI responses (identical inputs): 24 hours         │
│                                                          │
│  4. Database Query Cache (PostgreSQL)                   │
│     - Materialized views for complex analytics          │
│     - Refresh every 1 hour                              │
└─────────────────────────────────────────────────────────┘
```

---

## Monitoring & Observability

```
┌─────────────────────────────────────────────────────────┐
│                   APPLICATION METRICS                    │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Django Middleware → Structured Logs (JSON)             │
│  ├─ Request ID                                          │
│  ├─ User ID                                             │
│  ├─ Endpoint                                            │
│  ├─ Response Time                                       │
│  ├─ Status Code                                         │
│  └─ Errors                                              │
│                                                          │
│  Celery Tasks → Task Logs                               │
│  ├─ Task ID                                             │
│  ├─ Task Name                                           │
│  ├─ Execution Time                                      │
│  ├─ Success/Failure                                     │
│  └─ Retry Count                                         │
│                                                          │
│  AI Provider Calls → Cost Tracking                      │
│  ├─ Provider (OpenAI, Claude, Gemini)                   │
│  ├─ Model Used                                          │
│  ├─ Tokens Consumed                                     │
│  ├─ Cost ($)                                            │
│  └─ Response Time                                       │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                    LOGGING PIPELINE                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Application Logs → File System / CloudWatch / ELK      │
│                                                          │
│  Error Tracking → Sentry (recommended)                  │
│  ├─ Crash reports                                       │
│  ├─ Performance issues                                  │
│  └─ User feedback                                       │
│                                                          │
│  Analytics → Custom Dashboard                           │
│  ├─ Daily Active Users                                  │
│  ├─ Transactions per Day                                │
│  ├─ Chat Usage Rate                                     │
│  ├─ AI Parsing Accuracy                                 │
│  └─ Statement Upload Success Rate                       │
└─────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                      PRODUCTION                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │         CDN (CloudFlare / CloudFront)          │    │
│  │         - Static files                         │    │
│  │         - React app bundles                    │    │
│  └────────────────────────────────────────────────┘    │
│                         │                               │
│                         ▼                               │
│  ┌────────────────────────────────────────────────┐    │
│  │     Load Balancer (AWS ALB / Nginx)            │    │
│  └────────────────────────────────────────────────┘    │
│                         │                               │
│        ┌────────────────┴─────────────────┐            │
│        │                                   │            │
│        ▼                                   ▼            │
│  ┌──────────────┐                   ┌──────────────┐  │
│  │  Web Servers │                   │  API Servers │  │
│  │  (Django)    │                   │  (Django)    │  │
│  │  - Gunicorn  │                   │  - Gunicorn  │  │
│  │  - 4 workers │                   │  - 8 workers │  │
│  └──────────────┘                   └──────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐ │
│  │            Background Workers (Celery)            │ │
│  │  - 10 general workers                            │ │
│  │  - 5 AI workers (GPU instances)                  │ │
│  │  - 3 email workers                               │ │
│  └──────────────────────────────────────────────────┘ │
│                                                         │
│  ┌────────────────┐  ┌───────────────┐  ┌──────────┐ │
│  │  PostgreSQL    │  │  Redis        │  │   S3     │ │
│  │  - Primary     │  │  - Cache      │  │  - Files │ │
│  │  - Replica x2  │  │  - Sessions   │  │          │ │
│  └────────────────┘  └───────────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

This architecture supports:
- ✅ High availability (multi-instance deployment)
- ✅ Horizontal scalability (add more workers/servers)
- ✅ Performance (caching at multiple layers)
- ✅ Security (encryption, auth, row-level security)
- ✅ Observability (comprehensive logging and monitoring)
- ✅ Cost efficiency (Redis caching, CDN for static assets)